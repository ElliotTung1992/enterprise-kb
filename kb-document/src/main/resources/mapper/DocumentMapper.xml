<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.document.mapper.DocumentMapper">

    <resultMap id="DocumentResultMap" type="com.enterprise.kb.document.model.Document">
        <id property="id" column="id"/>
        <result property="spaceId" column="space_id"/>
        <result property="uploadedBy" column="uploaded_by"/>
        <result property="title" column="title"/>
        <result property="originalFilename" column="original_filename"/>
        <result property="filePath" column="file_path"/>
        <result property="fileSizeBytes" column="file_size_bytes"/>
        <result property="mimeType" column="mime_type"/>
        <result property="sourceUrl" column="source_url"/>
        <result property="status" column="status"/>
        <result property="errorMessage" column="error_message"/>
        <result property="chunkCount" column="chunk_count"/>
        <result property="language" column="language"/>
        <result property="metadata" column="metadata"
                typeHandler="com.enterprise.kb.document.typehandler.JsonbTypeHandler"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findByIdAndDeletedAtIsNull" resultMap="DocumentResultMap">
        SELECT * FROM documents WHERE id = #{id,jdbcType=OTHER} AND deleted_at IS NULL LIMIT 1
    </select>

    <select id="findBySpaceId" resultMap="DocumentResultMap">
        SELECT * FROM documents
        WHERE space_id = #{spaceId,jdbcType=OTHER} AND deleted_at IS NULL
        <if test="status != null">AND status = #{status}</if>
        <if test="keyword != null and keyword != ''">AND LOWER(title) LIKE LOWER(CONCAT('%', #{keyword}, '%'))</if>
        ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO documents (
            id, space_id, uploaded_by, title, original_filename, file_path,
            file_size_bytes, mime_type, source_url, status, error_message,
            chunk_count, language, metadata, created_at, updated_at, deleted_at
        ) VALUES (
            #{id,jdbcType=OTHER},
            #{spaceId,jdbcType=OTHER},
            #{uploadedBy,jdbcType=OTHER},
            #{title},
            #{originalFilename},
            #{filePath},
            #{fileSizeBytes},
            #{mimeType},
            #{sourceUrl},
            #{status},
            #{errorMessage},
            #{chunkCount},
            #{language},
            #{metadata,typeHandler=com.enterprise.kb.document.typehandler.JsonbTypeHandler,jdbcType=OTHER},
            #{createdAt},
            #{updatedAt},
            #{deletedAt}
        )
    </insert>

    <update id="update">
        UPDATE documents SET
            title = #{title},
            status = #{status},
            error_message = #{errorMessage},
            chunk_count = #{chunkCount},
            language = #{language},
            metadata = #{metadata,typeHandler=com.enterprise.kb.document.typehandler.JsonbTypeHandler,jdbcType=OTHER},
            updated_at = #{updatedAt},
            deleted_at = #{deletedAt}
        WHERE id = #{id,jdbcType=OTHER}
    </update>

</mapper>
