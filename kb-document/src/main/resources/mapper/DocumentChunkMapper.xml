<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.document.mapper.DocumentChunkMapper">

    <resultMap id="DocumentChunkResultMap" type="com.enterprise.kb.document.model.DocumentChunk">
        <id property="id" column="id"/>
        <result property="documentId" column="document_id"/>
        <result property="chunkIndex" column="chunk_index"/>
        <result property="content" column="content"/>
        <result property="tokenCount" column="token_count"/>
        <result property="milvusId" column="milvus_id"/>
        <result property="embeddingModel" column="embedding_model"/>
        <result property="pageNumber" column="page_number"/>
        <result property="charStart" column="char_start"/>
        <result property="charEnd" column="char_end"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByDocumentIdOrderByChunkIndex" resultMap="DocumentChunkResultMap">
        SELECT * FROM document_chunks
        WHERE document_id = #{documentId,jdbcType=OTHER}
        ORDER BY chunk_index ASC
    </select>

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO document_chunks (
            id, document_id, chunk_index, content, token_count,
            milvus_id, embedding_model, page_number, char_start, char_end, created_at
        ) VALUES (
            #{id,jdbcType=OTHER},
            #{documentId,jdbcType=OTHER},
            #{chunkIndex},
            #{content},
            #{tokenCount},
            #{milvusId},
            #{embeddingModel},
            #{pageNumber},
            #{charStart},
            #{charEnd},
            #{createdAt}
        )
    </insert>

    <insert id="insertBatch" useGeneratedKeys="false">
        INSERT INTO document_chunks (
            id, document_id, chunk_index, content, token_count,
            milvus_id, embedding_model, page_number, char_start, char_end, created_at
        ) VALUES
        <foreach collection="chunks" item="chunk" separator=",">
        (
            #{chunk.id,jdbcType=OTHER},
            #{chunk.documentId,jdbcType=OTHER},
            #{chunk.chunkIndex},
            #{chunk.content},
            #{chunk.tokenCount},
            #{chunk.milvusId},
            #{chunk.embeddingModel},
            #{chunk.pageNumber},
            #{chunk.charStart},
            #{chunk.charEnd},
            #{chunk.createdAt}
        )
        </foreach>
    </insert>

    <select id="countByDocumentId" resultType="long">
        SELECT COUNT(*) FROM document_chunks WHERE document_id = #{documentId,jdbcType=OTHER}
    </select>

    <delete id="deleteByDocumentId">
        DELETE FROM document_chunks WHERE document_id = #{documentId,jdbcType=OTHER}
    </delete>

    <delete id="deleteBySpaceId">
        DELETE FROM document_chunks
        WHERE document_id IN (SELECT id FROM documents WHERE space_id = #{spaceId,jdbcType=OTHER})
    </delete>

</mapper>
