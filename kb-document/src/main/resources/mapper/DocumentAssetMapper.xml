<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.document.mapper.DocumentAssetMapper">

    <resultMap id="DocumentAssetResultMap" type="com.enterprise.kb.document.model.DocumentAsset">
        <id property="id" column="id"/>
        <result property="documentId" column="document_id"/>
        <result property="assetType" column="asset_type"/>
        <result property="diagramType" column="diagram_type"/>
        <result property="assetIndex" column="asset_index"/>
        <result property="originalPath" column="original_path"/>
        <result property="objectKey" column="object_key"/>
        <result property="thumbnailObjectKey" column="thumbnail_object_key"/>
        <result property="mimeType" column="mime_type"/>
        <result property="fileSize" column="file_size"/>
        <result property="section" column="section"/>
        <result property="anchorChunkIndex" column="anchor_chunk_index"/>
        <result property="altText" column="alt_text"/>
        <result property="title" column="title"/>
        <result property="sourceCode" column="source_code"/>
        <result property="ocrText" column="ocr_text"/>
        <result property="caption" column="caption"/>
        <result property="summary" column="summary"/>
        <result property="manualCaption" column="manual_caption"/>
        <result property="manualSummary" column="manual_summary"/>
        <result property="status" column="status"/>
        <result property="retryCount" column="retry_count"/>
        <result property="nextRetryAt" column="next_retry_at"/>
        <result property="lastError" column="last_error"/>
        <result property="contentHash" column="content_hash"/>
        <result property="metadata" column="metadata"
                typeHandler="com.enterprise.kb.document.typehandler.JsonbTypeHandler"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insertBatch" useGeneratedKeys="false">
        INSERT INTO document_assets (
            id, document_id, asset_type, diagram_type, asset_index,
            original_path, object_key, thumbnail_object_key, mime_type, file_size,
            section, anchor_chunk_index, alt_text, title, source_code,
            ocr_text, caption, summary, manual_caption, manual_summary,
            status, retry_count, next_retry_at, last_error, content_hash,
            metadata, created_at, updated_at
        ) VALUES
        <foreach collection="assets" item="asset" separator=",">
        (
            #{asset.id,jdbcType=OTHER},
            #{asset.documentId,jdbcType=OTHER},
            #{asset.assetType},
            #{asset.diagramType},
            #{asset.assetIndex},
            #{asset.originalPath},
            #{asset.objectKey},
            #{asset.thumbnailObjectKey},
            #{asset.mimeType},
            #{asset.fileSize},
            #{asset.section},
            #{asset.anchorChunkIndex},
            #{asset.altText},
            #{asset.title},
            #{asset.sourceCode},
            #{asset.ocrText},
            #{asset.caption},
            #{asset.summary},
            #{asset.manualCaption},
            #{asset.manualSummary},
            #{asset.status},
            #{asset.retryCount},
            #{asset.nextRetryAt},
            #{asset.lastError},
            #{asset.contentHash},
            #{asset.metadata,typeHandler=com.enterprise.kb.document.typehandler.JsonbTypeHandler,jdbcType=OTHER},
            #{asset.createdAt},
            #{asset.updatedAt}
        )
        </foreach>
    </insert>

    <select id="findByDocumentId" resultMap="DocumentAssetResultMap">
        SELECT * FROM document_assets
        WHERE document_id = #{documentId,jdbcType=OTHER}
        ORDER BY asset_index ASC
    </select>

    <delete id="deleteByDocumentId">
        DELETE FROM document_assets WHERE document_id = #{documentId,jdbcType=OTHER}
    </delete>

    <delete id="deleteBySpaceId">
        DELETE FROM document_assets
        WHERE document_id IN (SELECT id FROM documents WHERE space_id = #{spaceId,jdbcType=OTHER})
    </delete>
</mapper>
