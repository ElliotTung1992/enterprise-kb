<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.user.mapper.SpaceMapper">

    <resultMap id="SpaceResultMap" type="com.enterprise.kb.user.model.Space">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="slug" column="slug"/>
        <result property="description" column="description"/>
        <result property="ownerId" column="owner_id"/>
        <result property="preferredModelProvider" column="preferred_model_provider"/>
        <result property="active" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findBySlugAndDeletedAtIsNull" resultMap="SpaceResultMap">
        SELECT * FROM spaces WHERE slug = #{slug} AND deleted_at IS NULL LIMIT 1
    </select>

    <select id="existsBySlugAndDeletedAtIsNull" resultType="boolean">
        SELECT COUNT(*) > 0 FROM spaces WHERE slug = #{slug} AND deleted_at IS NULL
    </select>

    <select id="findByIdAndDeletedAtIsNull" resultMap="SpaceResultMap">
        SELECT * FROM spaces WHERE id = #{id,jdbcType=OTHER} AND deleted_at IS NULL LIMIT 1
    </select>

    <select id="findAccessibleByUserId" resultMap="SpaceResultMap">
        SELECT s.* FROM spaces s
        WHERE s.deleted_at IS NULL
          AND s.id IN (
              SELECT usr.space_id FROM user_space_roles usr
              WHERE usr.user_id = #{userId,jdbcType=OTHER}
          )
    </select>

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO spaces (id, name, slug, description, owner_id, preferred_model_provider, is_active, created_at, updated_at, deleted_at)
        VALUES (
            #{id,jdbcType=OTHER},
            #{name},
            #{slug},
            #{description},
            #{ownerId,jdbcType=OTHER},
            #{preferredModelProvider},
            #{active},
            #{createdAt},
            #{updatedAt},
            #{deletedAt}
        )
    </insert>

    <update id="update">
        UPDATE spaces SET
            name = #{name},
            slug = #{slug},
            description = #{description},
            owner_id = #{ownerId,jdbcType=OTHER},
            preferred_model_provider = #{preferredModelProvider},
            is_active = #{active},
            updated_at = #{updatedAt},
            deleted_at = #{deletedAt}
        WHERE id = #{id,jdbcType=OTHER}
    </update>

</mapper>
