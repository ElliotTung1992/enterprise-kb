<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.enterprise.kb.user.model.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="fullName" column="full_name"/>
        <result property="active" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id,jdbcType=OTHER}
    </select>

    <select id="findByUsernameAndDeletedAtIsNull" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username} AND deleted_at IS NULL LIMIT 1
    </select>

    <select id="findByEmailAndDeletedAtIsNull" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email} AND deleted_at IS NULL LIMIT 1
    </select>

    <select id="existsByUsernameAndDeletedAtIsNull" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username} AND deleted_at IS NULL
    </select>

    <select id="existsByEmailAndDeletedAtIsNull" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findAllActive" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (LOWER(username) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
            OR LOWER(email) LIKE LOWER(CONCAT('%', #{keyword}, '%')))
        </if>
        ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO users (id, username, email, password_hash, full_name, is_active, created_at, updated_at, deleted_at)
        VALUES (
            #{id,jdbcType=OTHER},
            #{username},
            #{email},
            #{passwordHash},
            #{fullName},
            #{active},
            #{createdAt},
            #{updatedAt},
            #{deletedAt}
        )
    </insert>

    <update id="update">
        UPDATE users SET
            username = #{username},
            email = #{email},
            password_hash = #{passwordHash},
            full_name = #{fullName},
            is_active = #{active},
            updated_at = #{updatedAt},
            deleted_at = #{deletedAt}
        WHERE id = #{id,jdbcType=OTHER}
    </update>

</mapper>
