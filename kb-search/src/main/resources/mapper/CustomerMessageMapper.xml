<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.search.mapper.CustomerMessageMapper">

    <select id="findBySessionId" resultType="com.enterprise.kb.search.dto.CustomerMessageDto">
        SELECT id, role, content, domain, created_at AS createdAt
        FROM customer_messages
        WHERE session_id = #{sessionId,jdbcType=OTHER}
        ORDER BY created_at ASC
    </select>

    <insert id="insert">
        INSERT INTO customer_messages (id, session_id, role, content, domain)
        VALUES (gen_random_uuid(), #{sessionId,jdbcType=OTHER}, #{role}, #{content}, #{domain})
    </insert>

</mapper>
