<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.search.mapper.AgentTraceMapper">

    <resultMap id="AgentTraceResultMap" type="com.enterprise.kb.search.model.AgentTrace">
        <id property="id" column="id"/>
        <result property="traceType" column="trace_type"/>
        <result property="sessionId" column="session_id"/>
        <result property="spaceId" column="space_id"/>
        <result property="userId" column="user_id"/>
        <result property="requestId" column="request_id"/>
        <result property="modelProvider" column="model_provider"/>
        <result property="status" column="status"/>
        <result property="inputText" column="input_text"/>
        <result property="outputText" column="output_text"/>
        <result property="rawInputJson" column="raw_input_json"/>
        <result property="rawOutputJson" column="raw_output_json"/>
        <result property="durationMs" column="duration_ms"/>
        <result property="tokensUsed" column="tokens_used"/>
        <result property="errorType" column="error_type"/>
        <result property="errorMessage" column="error_message"/>
        <result property="payloadTruncated" column="payload_truncated"/>
        <result property="startedAt" column="started_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findById" resultMap="AgentTraceResultMap">
        SELECT *
        FROM agent_traces
        WHERE id = #{id,jdbcType=OTHER}
    </select>

    <select id="findByFilters" resultMap="AgentTraceResultMap">
        SELECT *
        FROM agent_traces
        <where>
            <if test="spaceId != null">
                AND space_id = #{spaceId,jdbcType=OTHER}
            </if>
            <if test="sessionId != null">
                AND session_id = #{sessionId,jdbcType=OTHER}
            </if>
            <if test="traceType != null and traceType != ''">
                AND trace_type = #{traceType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY started_at DESC
        LIMIT #{limit}
    </select>

    <insert id="insert">
        INSERT INTO agent_traces (
            id, trace_type, session_id, space_id, user_id, request_id,
            model_provider, status, input_text, output_text,
            raw_input_json, raw_output_json, duration_ms, tokens_used,
            error_type, error_message, payload_truncated,
            started_at, completed_at, created_at
        ) VALUES (
            #{id,jdbcType=OTHER},
            #{traceType},
            #{sessionId,jdbcType=OTHER},
            #{spaceId,jdbcType=OTHER},
            #{userId,jdbcType=OTHER},
            #{requestId},
            #{modelProvider},
            #{status},
            #{inputText},
            #{outputText},
            #{rawInputJson,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.StringTypeHandler}::jsonb,
            #{rawOutputJson,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.StringTypeHandler}::jsonb,
            #{durationMs},
            #{tokensUsed},
            #{errorType},
            #{errorMessage},
            #{payloadTruncated},
            #{startedAt},
            #{completedAt},
            #{createdAt}
        )
    </insert>

    <update id="complete">
        UPDATE agent_traces
        SET status = #{status},
            output_text = #{outputText},
            raw_output_json = #{rawOutputJson,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.StringTypeHandler}::jsonb,
            duration_ms = #{durationMs},
            tokens_used = #{tokensUsed},
            payload_truncated = payload_truncated OR #{payloadTruncated},
            completed_at = #{completedAt}
        WHERE id = #{id,jdbcType=OTHER}
    </update>

    <update id="fail">
        UPDATE agent_traces
        SET status = #{status},
            error_type = #{errorType},
            error_message = #{errorMessage},
            duration_ms = #{durationMs},
            completed_at = #{completedAt}
        WHERE id = #{id,jdbcType=OTHER}
    </update>

</mapper>
