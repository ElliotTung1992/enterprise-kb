<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.search.mapper.ComplaintPlanMapper">

    <resultMap id="ComplaintPlanResultMap" type="com.enterprise.kb.search.model.ComplaintPlan">
        <id property="id" column="id"/>
        <result property="complaintId" column="complaint_id"/>
        <result property="responsibleParty" column="responsible_party"/>
        <result property="responsibilityReason" column="responsibility_reason"/>
        <result property="compensationType" column="compensation_type"/>
        <result property="compensationAmount" column="compensation_amount"/>
        <result property="contactSequence" column="contact_sequence"/>
        <result property="timeouts" column="timeouts"/>
        <result property="fallbackPlan" column="fallback_plan"/>
        <result property="replanCount" column="replan_count"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="ComplaintPlanResultMap">
        SELECT *
        FROM complaint_plans
        WHERE id = #{id,jdbcType=OTHER}
    </select>

    <select id="findByComplaintId" resultMap="ComplaintPlanResultMap">
        SELECT *
        FROM complaint_plans
        WHERE complaint_id = #{complaintId,jdbcType=OTHER}
        ORDER BY created_at ASC
    </select>

    <insert id="insert">
        INSERT INTO complaint_plans (
            id, complaint_id, responsible_party, responsibility_reason,
            compensation_type, compensation_amount, contact_sequence,
            timeouts, fallback_plan, replan_count, status, created_at, updated_at
        ) VALUES (
            #{id,jdbcType=OTHER},
            #{complaintId,jdbcType=OTHER},
            #{responsibleParty},
            #{responsibilityReason},
            #{compensationType},
            #{compensationAmount},
            #{contactSequence,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.StringTypeHandler}::jsonb,
            #{timeouts,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.StringTypeHandler}::jsonb,
            #{fallbackPlan,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.StringTypeHandler}::jsonb,
            #{replanCount},
            #{status},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <update id="updateStatus">
        UPDATE complaint_plans
        SET status = #{status},
            updated_at = #{updatedAt}
        WHERE id = #{id,jdbcType=OTHER}
    </update>

    <update id="updateFields">
        UPDATE complaint_plans
        <set>
            updated_at = #{updatedAt},
            <if test="responsibleParty != null">responsible_party = #{responsibleParty},</if>
            <if test="compensationType != null">compensation_type = #{compensationType},</if>
            <if test="compensationAmount != null">compensation_amount = #{compensationAmount},</if>
        </set>
        WHERE id = #{id,jdbcType=OTHER}
    </update>

    <select id="findByStatus" resultMap="ComplaintPlanResultMap">
        SELECT *
        FROM complaint_plans
        <where>
            <if test="status != null">status = #{status}</if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>
