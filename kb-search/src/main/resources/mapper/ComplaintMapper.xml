<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.search.mapper.ComplaintMapper">

    <resultMap id="ComplaintResultMap" type="com.enterprise.kb.search.model.Complaint">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="orderId" column="order_id"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="resolvedAt" column="resolved_at"/>
    </resultMap>

    <select id="findById" resultMap="ComplaintResultMap">
        SELECT *
        FROM complaints
        WHERE id = #{id,jdbcType=OTHER}
    </select>

    <insert id="insert">
        INSERT INTO complaints (
            id, user_id, order_id, content, status, created_at, updated_at, resolved_at
        ) VALUES (
            #{id,jdbcType=OTHER},
            #{userId,jdbcType=OTHER},
            #{orderId},
            #{content},
            #{status},
            #{createdAt},
            #{updatedAt},
            #{resolvedAt}
        )
    </insert>

    <update id="updateStatus">
        UPDATE complaints
        SET status = #{status},
            updated_at = #{updatedAt},
            resolved_at = CASE
                WHEN #{status} IN ('RESOLVED', 'CLOSED') THEN #{updatedAt}
                ELSE resolved_at
            END
        WHERE id = #{id,jdbcType=OTHER}
    </update>

</mapper>
