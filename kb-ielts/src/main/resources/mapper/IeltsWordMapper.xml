<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.kb.ielts.mapper.IeltsWordMapper">

    <resultMap id="BaseResultMap" type="com.enterprise.kb.ielts.model.IeltsWord" autoMapping="true">
        <id property="id" column="id" javaType="java.util.UUID" jdbcType="OTHER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, word, phonetic_uk, phonetic_us, part_of_speech, definition_zh, definition_en,
        example_sentence, example_translation, frequency_level, word_list, difficulty,
        skill_tags, topic_tags, created_at, updated_at
    </sql>

    <sql id="Where_Clause">
        <where>
            <if test="difficulty != null">AND difficulty = #{difficulty}</if>
            <if test="wordList != null and wordList != ''">AND word_list = #{wordList}</if>
            <if test="topicTags != null and topicTags != ''">AND topic_tags LIKE '%' || #{topicTags} || '%'</if>
        </where>
    </sql>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ielts_words
        WHERE id = #{id,jdbcType=OTHER}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ielts_words
        <include refid="Where_Clause"/>
        ORDER BY created_at DESC
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM ielts_words
        <include refid="Where_Clause"/>
    </select>

    <insert id="insert" parameterType="com.enterprise.kb.ielts.model.IeltsWord">
        INSERT INTO ielts_words (id, word, phonetic_uk, phonetic_us, part_of_speech,
            definition_zh, definition_en, example_sentence, example_translation,
            frequency_level, word_list, difficulty, skill_tags, topic_tags, created_at, updated_at)
        VALUES (#{id,jdbcType=OTHER}, #{word}, #{phoneticUk}, #{phoneticUs}, #{partOfSpeech},
            #{definitionZh}, #{definitionEn}, #{exampleSentence}, #{exampleTranslation},
            #{frequencyLevel}, #{wordList}, #{difficulty}, #{skillTags}, #{topicTags},
            #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.enterprise.kb.ielts.model.IeltsWord">
        UPDATE ielts_words SET
            word = #{word},
            phonetic_uk = #{phoneticUk},
            phonetic_us = #{phoneticUs},
            part_of_speech = #{partOfSpeech},
            definition_zh = #{definitionZh},
            definition_en = #{definitionEn},
            example_sentence = #{exampleSentence},
            example_translation = #{exampleTranslation},
            frequency_level = #{frequencyLevel},
            word_list = #{wordList},
            difficulty = #{difficulty},
            skill_tags = #{skillTags},
            topic_tags = #{topicTags},
            updated_at = #{updatedAt}
        WHERE id = #{id,jdbcType=OTHER}
    </update>

    <delete id="deleteById">
        DELETE FROM ielts_words WHERE id = #{id,jdbcType=OTHER}
    </delete>

    <insert id="batchInsert">
        INSERT INTO ielts_words (id, word, phonetic_uk, phonetic_us, part_of_speech,
            definition_zh, definition_en, example_sentence, example_translation,
            frequency_level, word_list, difficulty, skill_tags, topic_tags, created_at, updated_at)
        VALUES
        <foreach collection="list" item="w" separator=",">
            (#{w.id,jdbcType=OTHER}, #{w.word}, #{w.phoneticUk}, #{w.phoneticUs}, #{w.partOfSpeech},
             #{w.definitionZh}, #{w.definitionEn}, #{w.exampleSentence}, #{w.exampleTranslation},
             #{w.frequencyLevel}, #{w.wordList}, #{w.difficulty}, #{w.skillTags}, #{w.topicTags},
             #{w.createdAt}, #{w.updatedAt})
        </foreach>
    </insert>

</mapper>
